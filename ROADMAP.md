# 다음에 추가할 기능 (지금은 작업하지 않음)

사용자가 나중에 진행해달라고 요청한 순서대로 기록.

1. ~~**세금 계산**~~ — 완료. `category.tax3_3` / `category.taxInsurance` 체크박스(3.3% 원천징수, 사대보험)로 근무처별 개별 설정, `lib/pay.js`의 `calculateNetPay()`가 최종 지급액에 반영.
2. ~~**주휴수당 계산**~~ — 완료. 일~토 주 단위로 근무시간을 묶어 15시간 이상이면 `(주 근무시간/40)*8*시급`을 지급(40시간 초과분 미반영), 세금 설정도 함께 적용. 근무처별 카드에 주 단위 표로 표시. 개근/다음 주 근로 예정 여부는 판단 불가하므로 "기록이 있으면 개근으로 간주"하는 것으로 안내 문구 명시.
3. **카카오/구글 소셜 로그인** — 현재는 로그인 없이 로컬스토리지 기반. 로그인 도입 시 데이터 저장 방식(서버/클라우드 동기화 여부)도 함께 결정 필요.
4. **반복 일정 등록** — 특정 요일/기간 반복되는 근무 스케줄을 한 번에 등록.
5. ~~**새 근무처(범주) 추가 UI 분리**~~ — 완료. "근무처" 라벨 옆 `(+)` 버튼을 눌러야만 이름/시급/세금 입력 패널이 펼쳐지도록 변경.

## 참고
- 데이터 스키마를 바꿀 때는 `src/lib/useSchedules.js`의 `loadSchedules()`처럼 기존 저장 데이터를 새 형식으로 마이그레이션하는 로직을 반드시 함께 추가할 것 (예전 형식 데이터를 통째로 깨뜨려 앱이 죽었던 사례 있음).
